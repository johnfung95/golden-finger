import React from "react";
import classes from "./Button.module.css";

const Button = () => {
  return (
    <div>
      <button className={classes.btn}>Hit me!</button>
    </div>
  );
};

export default Button;
